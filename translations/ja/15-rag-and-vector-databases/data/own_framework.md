<!--
CO_OP_TRANSLATOR_METADATA:
{
  "original_hash": "df98b2c59f87d8543135301e87969f70",
  "translation_date": "2025-06-25T23:18:58+00:00",
  "source_file": "15-rag-and-vector-databases/data/own_framework.md",
  "language_code": "ja"
}
-->
# ニューラルネットワークの紹介。多層パーセプトロン

前のセクションでは、最も簡単なニューラルネットワークモデルである一層パーセプトロン、線形の二クラス分類モデルについて学びました。

このセクションでは、このモデルをより柔軟なフレームワークに拡張し、以下を可能にします：

* 二クラスに加えて**多クラス分類**を行う
* 分類に加えて**回帰問題**を解決する
* 線形で分離できないクラスを分離する

また、Pythonで独自のモジュラーフレームワークを開発し、さまざまなニューラルネットワークアーキテクチャを構築できるようにします。

## 機械学習の形式化

機械学習の問題を形式化することから始めましょう。トレーニングデータセット**X**とラベル**Y**があると仮定し、最も正確な予測を行うモデル*f*を構築する必要があります。予測の品質は**損失関数**ℒで測定されます。以下の損失関数がよく使われます：

* 数値を予測する必要がある回帰問題の場合、**絶対誤差**∑<sub>i</sub>|f(x<sup>(i)</sup>)-y<sup>(i)</sup>|や**二乗誤差**∑<sub>i</sub>(f(x<sup>(i)</sup>)-y<sup>(i)</sup>)<sup>2</sup>を使用できます
* 分類の場合、**0-1損失**（本質的にはモデルの**精度**と同じ）や**ロジスティック損失**を使用します。

一層パーセプトロンの場合、関数*f*は線形関数*f(x)=wx+b*として定義されました（ここで*w*は重み行列、*x*は入力特徴のベクトル、*b*はバイアスベクトルです）。異なるニューラルネットワークアーキテクチャでは、この関数はより複雑な形を取ることができます。

> 分類の場合、対応するクラスの確率をネットワークの出力として得ることが望ましいことがよくあります。任意の数値を確率に変換するために（例えば出力を正規化するために）、**ソフトマックス**関数σをよく使用し、関数*f*は*f(x)=σ(wx+b)*となります。

上記の*f*の定義では、*w*と*b*は**パラメータ**θ=⟨*w,b*⟩と呼ばれます。データセット⟨**X**,**Y**⟩が与えられると、パラメータθの関数としてデータセット全体の誤差を計算できます。

> ✅ **ニューラルネットワークのトレーニングの目的は、パラメータθを変化させることで誤差を最小化することです**

## 勾配降下法による最適化

関数最適化のよく知られた方法として**勾配降下法**があります。アイデアは、パラメータに対する損失関数の微分（多次元の場合は**勾配**と呼ばれる）を計算し、誤差が減少するようにパラメータを変化させることです。これを次のように形式化できます：

* パラメータをランダムな値で初期化する w<sup>(0)</sup>, b<sup>(0)</sup>
* 次のステップを何度も繰り返す：
    - w<sup>(i+1)</sup> = w<sup>(i)</sup>-η∂ℒ/∂w
    - b<sup>(i+1)</sup> = b<sup>(i)</sup>-η∂ℒ/∂b

トレーニング中、最適化ステップはデータセット全体を考慮して計算されることが期待されます（損失はすべてのトレーニングサンプルを通じての合計として計算されることを思い出してください）。しかし、実際にはデータセットの小さな部分を**ミニバッチ**と呼ばれる形で取り、データのサブセットに基づいて勾配を計算します。サブセットは毎回ランダムに取られるため、この方法は**確率的勾配降下法**（SGD）と呼ばれます。

## 多層パーセプトロンとバックプロパゲーション

一層のネットワークは、上記で見たように、線形で分離可能なクラスを分類することができます。より豊かなモデルを構築するために、ネットワークのいくつかの層を組み合わせることができます。数学的には、関数*f*がより複雑な形を取り、いくつかのステップで計算されることを意味します：
* z<sub>1</sub>=w<sub>1</sub>x+b<sub>1</sub>
* z<sub>2</sub>=w<sub>2</sub>α(z<sub>1</sub>)+b<sub>2</sub>
* f = σ(z<sub>2</sub>)

ここで、αは**非線形活性化関数**、σはソフトマックス関数、パラメータθ=<*w<sub>1</sub>,b<sub>1</sub>,w<sub>2</sub>,b<sub>2</sub>*>です。

勾配降下アルゴリズムは同じままですが、勾配を計算するのはより困難になります。チェーン微分のルールを考慮すると、微分を次のように計算できます：

* ∂ℒ/∂w<sub>2</sub> = (∂ℒ/∂σ)(∂σ/∂z<sub>2</sub>)(∂z<sub>2</sub>/∂w<sub>2</sub>)
* ∂ℒ/∂w<sub>1</sub> = (∂ℒ/∂σ)(∂σ/∂z<sub>2</sub>)(∂z<sub>2</sub>/∂α)(∂α/∂z<sub>1</sub>)(∂z<sub>1</sub>/∂w<sub>1</sub>)

> ✅ チェーン微分のルールは、パラメータに対する損失関数の微分を計算するために使用されます。

これらの表現の左端部分はすべて同じであるため、損失関数から始めて計算グラフを「逆向き」に進んで微分を効果的に計算できます。このため、多層パーセプトロンのトレーニング方法は**バックプロパゲーション**または「バックプロップ」と呼ばれます。

> TODO: 画像引用

> ✅ バックプロップについては、ノートブックの例でより詳しく説明します。

## 結論

このレッスンでは、独自のニューラルネットワークライブラリを構築し、簡単な二次元分類タスクに使用しました。

## 🚀 チャレンジ

付属のノートブックでは、多層パーセプトロンを構築しトレーニングするための独自のフレームワークを実装します。現代のニューラルネットワークがどのように動作するかを詳細に見ることができます。

OwnFrameworkノートブックに進み、それを進めてください。

## レビューと自己学習

バックプロパゲーションはAIとMLでよく使われるアルゴリズムであり、より詳しく学ぶ価値があります。

## 課題

このラボでは、このレッスンで構築したフレームワークを使用して、MNIST手書き数字分類を解決することが求められます。

* 手順
* ノートブック

**免責事項**:  
このドキュメントはAI翻訳サービス[Co-op Translator](https://github.com/Azure/co-op-translator)を使用して翻訳されています。正確さを期していますが、自動翻訳には誤りや不正確さが含まれる場合がありますのでご注意ください。元の言語のドキュメントが権威ある情報源と見なされるべきです。重要な情報については、専門の人間による翻訳をお勧めします。この翻訳の使用に起因する誤解や誤訳について、当社は一切の責任を負いません。
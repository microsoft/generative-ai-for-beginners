# نقشه راه ویژگی‌ها و بهبودهای پیشرفته

این سند به تشریح بهبودها و ارتقاءهای پیشنهادی برای درسنامه هوش مصنوعی مولد برای مبتدیان می‌پردازد که بر اساس بررسی جامع کد و تحلیل بهترین روش‌های صنعت تهیه شده است.

## خلاصه اجرایی

کد منبع از نظر امنیت، کیفیت کد و اثربخشی آموزشی مورد بررسی قرار گرفته است. این سند پیشنهاداتی برای رفع فوری، بهبودهای کوتاه‌مدت و ارتقاءهای آتی ارائه می‌دهد.

---

## ۱. بهبودهای امنیتی (اولویت: حیاتی)

### ۱.۱ رفع‌های فوری (انجام شده)

| مشکل | فایل‌های متاثر | وضعیت |
|-------|----------------|--------|
| کلید محرمانه سخت‌کد شده | `05-advanced-prompts/python/aoai-solution.py` | رفع شده |
| اعتبارسنجی env ناقص | چندین فایل JS/TS | رفع شده |
| فراخوانی‌های ناامن توابع | `11-integrating-with-function-calling/js-githubmodels/app.js` | رفع شده |
| نشت دسته فایل | `08-building-search-applications/scripts/` | رفع شده |
| نبود زمان‌سنجی درخواست | `09-building-image-applications/python/` | رفع شده |

### ۱.۲ ویژگی‌های امنیتی پیشنهادی اضافی

1. **نمونه‌های محدودیت نرخ**
   - افزودن کد نمونه برای پیاده‌سازی محدودیت نرخ تماس‌های API
   - نمایش الگوهای بازگشت نمایی

2. **چرخش کلید API**
   - افزودن مستندات درباره بهترین روش‌های چرخش کلیدهای API
   - شامل نمونه‌هایی برای استفاده از Azure Key Vault یا خدمات مشابه

3. **ادغام ایمنی محتوا**
   - افزودن نمونه‌هایی با استفاده از Azure Content Safety API
   - نمایش الگوهای مدیریت ورودی/خروجی

---

## ۲. بهبود کیفیت کد

### ۲.۱ فایل‌های پیکربندی افزوده شده

| فایل | هدف |
|------|---------|
| `.eslintrc.json` | قوانین linting جاوااسکریپت/تایپ‌اسکریپت |
| `.prettierrc` | استانداردهای قالب‌بندی کد |
| `pyproject.toml` | پیکربندی ابزارهای پایتون (Black, Ruff, mypy) |

### ۲.۲ ابزارهای مشترک ایجاد شده

ماژول جدید `shared/python/` با:
- `env_utils.py` - مدیریت متغیرهای محیطی
- `input_validation.py` - اعتبارسنجی و پاکسازی ورودی‌ها
- `api_utils.py` - بسته‌های ایمن برای درخواست‌های API

### ۲.۳ بهبودهای پیشنهادی کد

1. **پوشش Type Hints**
   - افزودن اشاره‌گرهای تایپی به همه فایل‌های پایتون
   - فعال‌سازی حالت سخت‌گیرانه TypeScript در همه پروژه‌های TS

2. **استانداردهای مستندسازی**
   - افزودن docstrings به همه توابع پایتون
   - افزودن نظرات JSDoc به همه توابع جاوااسکریپت/تایپ‌اسکریپت

3. **چارچوب تست**
   - افزودن پیکربندی pytest و نمونه تست‌ها
   - افزودن پیکربندی Jest برای جاوااسکریپت/تایپ‌اسکریپت

---

## ۳. بهبودهای آموزشی

### ۳.۱ موضوعات درسی جدید

1. **امنیت در برنامه‌های هوش مصنوعی** (درس پیشنهادی ۲۲)
   - حملات و دفاع در تزریق داده به prompt
   - مدیریت کلید API
   - مدیریت محتوا
   - محدودیت نرخ و جلوگیری از سوء استفاده

2. **استقرار در محیط تولید** (درس پیشنهادی ۲۳)
   - کانتینرسازی با Docker
   - خطوط CI/CD
   - نظارت و لاگ‌برداری
   - مدیریت هزینه

3. **تکنیک‌های پیشرفته RAG** (درس پیشنهادی ۲۴)
   - جستجوی ترکیبی (کلمات کلیدی + معنایی)
   - استراتژی‌های رتبه‌بندی مجدد
   - RAG چندرسانه‌ای
   - معیارهای ارزیابی

### ۳.۲ بهبود درس‌های موجود

| درس | بهبود پیشنهادی |
|--------|------------------------|
| ۰۶ - تولید متن | افزودن مثال‌های پاسخ استریمینگ |
| ۰۷ - برنامه‌های چت | افزودن الگوهای حافظه مکالمه |
| ۰۸ - برنامه‌های جستجو | افزودن مقایسه پایگاه داده برداری |
| ۰۹ - تولید تصویر | افزودن مثال‌های ویرایش/تنوع تصویر |
| ۱۱ - فراخوانی توابع | افزودن فراخوانی توابع موازی |
| ۱۵ - RAG | افزودن مقایسه استراتژی تقسیم‌بندی |
| ۱۷ - عوامل هوش مصنوعی | افزودن هماهنگی چندعامله |

---

## ۴. مدرن‌سازی API

### ۴.۱ الگوهای قدیمی API که باید به‌روزرسانی شوند

| الگوی قدیمی | الگوی جدید | فایل‌های متاثر |
|-------------|-------------|----------------|
| `openai.api_type = "azure"` | کلاینت `AzureOpenAI()` | چندین اسکریپت در `08-building-search-applications/` |
| `openai.ChatCompletion.create()` | `client.chat.completions.create()` | چند دفترچه یادداشت |
| `df.append()` (pandas) | `pd.concat()` | دفترچه RAG |

### ۴.۲ ویژگی‌های جدید API برای نمایش

1. **خروجی‌های ساختاریافته** (OpenAI)
   - حالت JSON
   - فراخوانی توابع با اسکیماهای سخت‌گیرانه

2. **قابلیت‌های بینایی**
   - تحلیل تصویر با GPT-4V
   - پرامپت‌های چندرسانه‌ای

3. **API دستیاران**
   - مفسر کد
   - جستجوی فایل
   - ابزارهای سفارشی

---

## ۵. بهبودهای زیرساخت

### ۵.۱ بهبودهای CI/CD

روندهای کاری فعلی اعتبارسنجی مارک‌داون را انجام می‌دهند. افزودنی‌های پیشنهادی:

```yaml
# .github/workflows/code-quality.yml
name: Code Quality

on: [push, pull_request]

jobs:
  python-lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.10'
      - run: pip install ruff black mypy
      - run: ruff check .
      - run: black --check .

  js-lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - run: npm ci
      - run: npx eslint .
```

### ۵.۲ اسکن امنیتی

```yaml
# .github/workflows/security.yml
name: Security Scan

on: [push, pull_request]

jobs:
  codeql:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: github/codeql-action/init@v3
        with:
          languages: javascript, python
      - uses: github/codeql-action/analyze@v3

  dependency-review:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/dependency-review-action@v4
```

---

## ۶. بهبود تجربه توسعه‌دهنده

### ۶.۱ بهبودهای DevContainer

به‌روزرسانی `.devcontainer/devcontainer.json`:

```json
{
  "name": "Generative AI for Beginners",
  "image": "mcr.microsoft.com/devcontainers/universal:2",
  "features": {
    "ghcr.io/devcontainers/features/python:1": {
      "version": "3.11"
    },
    "ghcr.io/devcontainers/features/node:1": {
      "version": "20"
    }
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-python.vscode-pylance",
        "ms-toolsai.jupyter",
        "dbaeumer.vscode-eslint",
        "esbenp.prettier-vscode",
        "github.copilot"
      ],
      "settings": {
        "python.formatting.provider": "black",
        "editor.formatOnSave": true
      }
    }
  },
  "postCreateCommand": "pip install -e .[dev] && npm install"
}
```

### ۶.۲ محیط تعاملی

در نظر گرفتن افزودن:
- دفترچه‌های Jupyter با کلیدهای API از پیش پرشده (از طریق محیط)
- دموهای Gradio/Streamlit برای یادگیرندگان بصری
- آزمون‌های تعاملی برای ارزیابی دانش

---

## ۷. پشتیبانی چندزبانه

### ۷.۱ پوشش زبان‌های فعلی

| فناوری | درس‌های پوشش داده شده | وضعیت |
|------------|-----------------|--------|
| پایتون | همه | کامل |
| تایپ‌اسکریپت | ۰۶-۰۹، ۱۱ | جزئی |
| جاوااسکریپت | ۰۶-۰۸، ۱۱ | جزئی |
| .NET/C# | برخی | جزئی |

### ۷.۲ افزودنی‌های پیشنهادی

1. **Go** - در حال رشد در ابزارهای AI/ML
2. **Rust** - برنامه‌های حساس به عملکرد
3. **Java/Kotlin** - برنامه‌های سازمانی

---

## ۸. بهینه‌سازی‌های عملکردی

### ۸.۱ بهینه‌سازی در سطح کد

1. **الگوهای Async/Await**
   - افزودن مثال‌های async برای پردازش دسته‌ای
   - نمایش فراخوانی‌های موازی API

2. **استراتژی‌های کشینگ**
   - افزودن مثال‌های کشینگ امبدینگ
   - نمایش الگوهای کشینگ پاسخ‌ها

3. **بهینه‌سازی توکن**
   - افزودن مثال‌های استفاده از tiktoken
   - نمایش تکنیک‌های فشرده‌سازی prompt

### ۸.۲ مثال‌های بهینه‌سازی هزینه

افزودن مثال‌هایی برای:
- انتخاب مدل بر اساس پیچیدگی کار
- مهندسی prompt برای بهره‌وری توکن
- پردازش دسته‌ای برای عملیات حجیم

---

## ۹. دسترسی‌پذیری و بین‌المللی‌سازی

### ۹.۱ وضعیت فعلی ترجمه

| زبان | وضعیت |
|----------|--------|
| انگلیسی | کامل |
| چینی (ساده‌شده) | کامل |
| ژاپنی | کامل |
| کره‌ای | کامل |
| اسپانیایی | جزئی |
| پرتغالی | جزئی |
| ترکی | جزئی |
| لهستانی | جزئی |

### ۹.۲ بهبودهای دسترسی‌پذیری

1. افزودن متن جایگزین برای همه تصاویر
2. اطمینان از برجسته‌سازی صحیح نحو کدها
3. افزودن رونوشت ویدیو برای همه محتوای ویدیویی
4. اطمینان از رعایت کنتراست رنگ بر اساس دستورالعمل‌های WCAG

---

## ۱۰. اولویت‌بندی پیاده‌سازی

### فاز ۱: فوری (هفته ۱-۲)
- [x] رفع مشکلات امنیتی حیاتی
- [x] افزودن پیکربندی کیفیت کد
- [x] ایجاد ابزارهای مشترک
- [x] مستندسازی راهنمای امنیت

### فاز ۲: کوتاه‌مدت (هفته ۳-۴)
- [ ] به‌روزرسانی الگوهای قدیمی API
- [ ] افزودن اشاره‌گرهای تایپ به همه فایل‌های پایتون
- [ ] افزودن فرآیندهای CI/CD برای کیفیت کد
- [ ] ایجاد روند کاری اسکن امنیت

### فاز ۳: میان‌مدت (ماه ۲-۳)
- [ ] افزودن درس امنیتی جدید
- [ ] افزودن درس استقرار در تولید
- [ ] بهبود پیکربندی DevContainer
- [ ] افزودن دموهای تعاملی

### فاز ۴: بلندمدت (ماه ۴+)
- [ ] افزودن درس پیشرفته RAG
- [ ] گسترش پوشش زبان‌ها
- [ ] افزودن مجموعه کامل تست
- [ ] ایجاد برنامه گواهینامه

---

## نتیجه‌گیری

این نقشه راه رویکردی ساختاریافته برای بهبود درسنامه هوش مصنوعی مولد برای مبتدیان ارائه می‌دهد. با پرداختن به نگرانی‌های امنیتی، مدرن‌سازی APIها و افزودن محتوای آموزشی، دوره دانشجویان را بهتر برای توسعه کاربردهای واقعی هوش مصنوعی آماده می‌کند.

برای سوالات یا مشارکت‌ها، لطفاً در مخزن گیت‌هاب یک issue باز کنید.

---

<!-- CO-OP TRANSLATOR DISCLAIMER START -->
**توضیحات مهم**:  
این سند با استفاده از سرویس ترجمه هوش مصنوعی [Co-op Translator](https://github.com/Azure/co-op-translator) ترجمه شده است. در حالی که ما تلاش می‌کنیم دقت بالایی داشته باشیم، لطفاً توجه داشته باشید که ترجمه‌های خودکار ممکن است شامل اشتباهات یا نادرستی‌هایی باشند. سند اصلی به زبان بومی خود به‌عنوان منبع معتبر شناخته شود. برای اطلاعات حیاتی، توصیه می‌شود از ترجمه حرفه‌ای انسانی استفاده گردد. ما مسئول هیچ گونه سوءتفاهم یا تفسیر نادرست ناشی از استفاده از این ترجمه نیستیم.
<!-- CO-OP TRANSLATOR DISCLAIMER END -->
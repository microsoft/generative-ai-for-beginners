[![Открытые модели](../../../translated_images/ru/17-lesson-banner.a5b918fb0920e4e6.webp)](https://youtu.be/yAXVW-lUINc?si=bOtW9nL6jc3XJgOM)

## Введение

AI-агенты представляют собой захватывающее развитие в области генеративного ИИ, позволяя крупным языковым моделям (LLM) эволюционировать из помощников в агентов, способных предпринимать действия. Фреймворки AI-агентов позволяют разработчикам создавать приложения, которые предоставляют LLM доступ к инструментам и управлению состоянием. Эти фреймворки также улучшают видимость, позволяя пользователям и разработчикам отслеживать действия, запланированные LLM, тем самым улучшая управление опытом.

В уроке будут рассмотрены следующие темы:

- Понимание, что такое AI-агент — что именно представляет собой AI-агент?
- Изучение пяти различных фреймворков AI-агентов — что делает их уникальными?
- Применение этих AI-агентов к различным сценариям использования — когда следует использовать AI-агентов?

## Цели обучения

После прохождения этого урока вы сможете:

- Объяснить, что такое AI-агенты и как их можно использовать.
- Понимать различия между некоторыми популярными фреймворками AI-агентов и их отличия.
- Понимать, как работают AI-агенты, чтобы создавать приложения с их использованием.

## Что такое AI-агенты?

AI-агенты — это очень захватывающая область в мире генеративного ИИ. Вместе с этим волнением иногда возникает путаница в терминах и их применении. Чтобы упростить и включить большинство инструментов, относящихся к AI-агентам, мы будем использовать такое определение:

AI-агенты позволяют крупным языковым моделям (LLM) выполнять задачи, предоставляя им доступ к **состоянию** и **инструментам**.

![Модель агента](../../../translated_images/ru/what-agent.21f2893bdfd01e6a.webp)

Давайте определим эти термины:

**Крупные языковые модели** — это модели, о которых идет речь в этом курсе, такие как GPT-3.5, GPT-4, Llama-2 и др.

**Состояние** — это контекст, в котором работает LLM. LLM использует контекст своих прошлых действий и текущий контекст, направляя свое принятие решений для последующих действий. Фреймворки AI-агентов облегчают разработчикам поддержку этого контекста.

**Инструменты** — чтобы выполнить задачу, которую пользователь запросил и которую LLM запланировала, LLM необходим доступ к инструментам. Примерами инструментов могут быть база данных, API, внешнее приложение или даже другая LLM!

Эти определения, надеемся, предоставят вам хорошую основу, когда мы рассмотрим их реализацию. Давайте исследуем несколько различных фреймворков AI-агентов:

## LangChain Agents

[LangChain Agents](https://python.langchain.com/docs/how_to/#agents?WT.mc_id=academic-105485-koreyst) — это реализация вышеприведенных определений.

Для управления **состоянием** он использует встроенную функцию под названием `AgentExecutor`. Она принимает определенного `агента` и доступные для него `инструменты`.

`AgentExecutor` также сохраняет историю чата, чтобы предоставить контекст общения.

![Агенты Langchain](../../../translated_images/ru/langchain-agents.edcc55b5d5c43716.webp)

LangChain предлагает [каталог инструментов](https://integrations.langchain.com/tools?WT.mc_id=academic-105485-koreyst), которые можно импортировать в ваше приложение для доступа LLM. Эти инструменты создаются сообществом и командой LangChain.

Затем вы можете определить эти инструменты и передать их в `AgentExecutor`.

Видимость — еще один важный аспект при обсуждении AI-агентов. Важно, чтобы разработчики приложений понимали, какой инструмент использует LLM и почему. Для этого команда LangChain разработала LangSmith.

## AutoGen

Следующий фреймворк AI-агентов, который мы обсудим, — [AutoGen](https://microsoft.github.io/autogen/?WT.mc_id=academic-105485-koreyst). Основной фокус AutoGen — это беседы. Агенты могут **вести диалог** и быть **настраиваемыми**.

**Ведение диалога —** LLM могут начинать и продолжать разговор с другой LLM для выполнения задачи. Это достигается созданием `AssistantAgents` и предоставлением им определенного системного сообщения.

```python

autogen.AssistantAgent( name="Coder", llm_config=llm_config, ) pm = autogen.AssistantAgent( name="Product_manager", system_message="Creative in software product ideas.", llm_config=llm_config, )

```

**Настраиваемость** — агенты могут определяться не только как LLM, но и как пользователь или инструмент. В качестве разработчика вы можете определить `UserProxyAgent`, ответственного за взаимодействие с пользователем для получения обратной связи при выполнении задачи. Эта обратная связь может либо продолжить выполнение задачи, либо остановить её.

```python
user_proxy = UserProxyAgent(name="user_proxy")
```

### Состояние и инструменты

Для изменения и управления состоянием помощник-агент генерирует Python-код для выполнения задачи.

Вот пример процесса:

![AutoGen](../../../translated_images/ru/autogen.dee9a25a45fde584.webp)

#### LLM определяется системным сообщением

```python
system_message="For weather related tasks, only use the functions you have been provided with. Reply TERMINATE when the task is done."
```

Это системное сообщение направляет конкретную LLM, какие функции релевантны для её задачи. Помните, в AutoGen вы можете иметь несколько AssistantAgents с разными системными сообщениями.

#### Инициирование чата пользователем

```python
user_proxy.initiate_chat( chatbot, message="I am planning a trip to NYC next week, can you help me pick out what to wear? ", )

```

Это сообщение от user_proxy (Человека) запускает процесс, в котором агент исследует возможные функции, которые он должен выполнить.

#### Выполнение функции

```bash
chatbot (to user_proxy):

***** Suggested tool Call: get_weather ***** Arguments: {"location":"New York City, NY","time_periond:"7","temperature_unit":"Celsius"} ******************************************************** --------------------------------------------------------------------------------

>>>>>>>> EXECUTING FUNCTION get_weather... user_proxy (to chatbot): ***** Response from calling function "get_weather" ***** 112.22727272727272 EUR ****************************************************************

```

После обработки первоначального чата агент отправит предложение вызвать инструмент. В этом случае это функция `get_weather`. В зависимости от вашей конфигурации, эта функция может быть автоматически выполнена и прочитана агентом или выполнена по запросу пользователя.

Вы можете найти список [примеров кода AutoGen](https://microsoft.github.io/autogen/docs/Examples/?WT.mc_id=academic-105485-koreyst), чтобы подробнее изучить, как начать создание.

## Microsoft Agent Framework

[Microsoft Agent Framework](https://learn.microsoft.com/agent-framework/?WT.mc_id=academic-105485-koreyst) — это открытый SDK от Microsoft для создания AI-агентов и мультиагентных систем как на **Python**, так и на **.NET**. Он объединяет сильные стороны двух предыдущих проектов Microsoft — корпоративные возможности **Semantic Kernel** и оркестрацию нескольких агентов **AutoGen** — в едином поддерживаемом фреймворке. Если вы начинаете новый проект агента сегодня, это рекомендуемый преемник AutoGen.

Фреймворк масштабируется от одного **чат-агента** до сложных **мультиагентных рабочих процессов**, и он интегрируется напрямую с Microsoft Foundry, Azure OpenAI и OpenAI. Он также предоставляет встроенную наблюдаемость через OpenTelemetry, чтобы вы могли точно отследить, что делают ваши агенты.

### Состояние и инструменты

**Состояние** — фреймворк управляет контекстом разговора с помощью **потоков (threads)**. Агент отслеживает историю сообщений (запросы пользователя, вызовы инструментов и их результаты), поэтому каждый ход строится на предыдущих. Потоки также могут сохраняться, что позволяет приостанавливать и возобновлять разговор позже.

**Инструменты** — вы предоставляете агенту инструменты, передавая обычные функции Python. Типизированные параметры автоматически преобразуются в схему, чтобы модель знала, как и когда вызывать их (вызовы функций). Фреймворк также поддерживает сервера Model Context Protocol (MCP) и размещенные инструменты, такие как интерпретатор кода.

Вот пример одного агента с настраиваемым инструментом:

```python
import asyncio
from typing import Annotated

from pydantic import Field
from agent_framework import Agent
from agent_framework.openai import OpenAIChatClient


def get_weather(
    location: Annotated[str, Field(description="The location to get the weather for.")],
) -> str:
    """Get the weather for a given location."""
    return f"The weather in {location} is sunny with a high of 22°C."


async def main():
    agent = Agent(
        client=OpenAIChatClient(),
        instructions="You are a helpful assistant that can answer weather questions.",
        tools=[get_weather],
    )

    response = await agent.run("What's the weather in Amsterdam?")
    print(response)


asyncio.run(main())
```

Для подключения к Azure OpenAI в Microsoft Foundry передайте конечную точку и учетные данные клиенту:

```python
from azure.identity.aio import AzureCliCredential
from agent_framework.openai import OpenAIChatClient

client = OpenAIChatClient(
    model="my-gpt-4o-deployment",
    azure_endpoint="https://my-resource.openai.azure.com",
    credential=AzureCliCredential(),
)
```

### Мультиагентные рабочие процессы

Фреймворк особенно выделяется возможностью оркестровки нескольких агентов вместе. Например, вы можете запускать агентов последовательно (каждый передает контекст следующему) или параллельно направлять задачи нескольким агентам и объединять их результаты:

```python
from agent_framework.orchestrations import SequentialBuilder, ConcurrentBuilder

# Запускайте агентов последовательно, передавая контекст разговора по цепочке
sequential = SequentialBuilder(participants=[researcher, writer, editor]).build()

# Запускайте агентов параллельно, затем объединяйте их ответы
concurrent = ConcurrentBuilder(participants=[analyst_a, analyst_b, analyst_c]).build()
```

Чтобы установить фреймворк и начать работу:

```bash
pip install agent-framework-core
# Необязательные интеграции
pip install agent-framework-openai       # OpenAI и Azure OpenAI
pip install agent-framework-foundry      # Microsoft Foundry
```

Вы можете подробнее изучить в [репозитории Microsoft Agent Framework](https://github.com/microsoft/agent-framework?WT.mc_id=academic-105485-koreyst) и [официальной документации](https://learn.microsoft.com/agent-framework/?WT.mc_id=academic-105485-koreyst).

## Taskweaver

Следующий фреймворк агентов, который мы рассмотрим, — [Taskweaver](https://microsoft.github.io/TaskWeaver/?WT.mc_id=academic-105485-koreyst). Его называют "code-first" агентом, потому что он работает не только со строками (`strings`), но и с DataFrame в Python. Это чрезвычайно полезно для задач анализа и генерации данных, таких как создание графиков и диаграмм или генерация случайных чисел.

### Состояние и инструменты

Для управления состоянием разговора TaskWeaver использует концепцию `Planner`. `Planner` — это LLM, которая принимает запрос пользователя и планирует задачи, которые нужно выполнить для выполнения этого запроса.

Для выполнения задач `Planner` получает доступ к набору инструментов, называемых `Plugins`. Это могут быть Python-классы или общий интерпретатор кода. Эти плагины хранятся в виде эмбеддингов, чтобы LLM могла лучше искать правильный плагин.

![Taskweaver](../../../translated_images/ru/taskweaver.da8559999267715a.webp)

Вот пример плагина для обнаружения аномалий:

```python
class AnomalyDetectionPlugin(Plugin): def __call__(self, df: pd.DataFrame, time_col_name: str, value_col_name: str):
```

Код проверяется перед выполнением. Еще одной функцией для управления контекстом в Taskweaver является `experience`. Experience позволяет сохранять контекст разговора на длительное время в YAML-файле. Это можно настроить так, чтобы LLM со временем улучшался в выполнении определенных задач, учитывая предыдущие разговоры.

## JARVIS

Последний фреймворк агентов, который мы рассмотрим, — [JARVIS](https://github.com/microsoft/JARVIS?tab=readme-ov-file&WT.mc_id=academic-105485-koreyst). Особенность JARVIS в том, что он использует LLM для управления `состоянием` разговора, а `инструментами` выступают другие AI-модели. Каждая из AI-моделей — специализированная модель, выполняющая определенные задачи, такие как обнаружение объектов, транскрипция или создание описаний к изображениям.

![JARVIS](../../../translated_images/ru/jarvis.762ddbadbd1a3a33.webp)

LLM, являясь универсальной моделью, получает запрос от пользователя и определяет конкретную задачу и любые аргументы/данные, необходимые для её выполнения.

```python
[{"task": "object-detection", "id": 0, "dep": [-1], "args": {"image": "e1.jpg" }}]
```

Затем LLM форматирует запрос таким образом, чтобы специализированная AI-модель могла его интерпретировать, например, в формате JSON. Когда AI-модель возвращает свой прогноз по задаче, LLM получает ответ.

Если для выполнения задачи требуется несколько моделей, LLM также интерпретирует ответы от этих моделей, прежде чем объединить их для составления ответа пользователю.

Ниже приведен пример того, как это работает, когда пользователь запрашивает описание и подсчет объектов на изображении:

## Задание

Чтобы продолжить изучать AI-агентов, вы можете создать с помощью Microsoft Agent Framework:

- Приложение, которое симулирует деловую встречу с разными отделами образовательного стартапа.
- Создайте системные сообщения, которые помогут LLM понять разные персоны и приоритеты, а также позволят пользователю представить новую идею продукта.
- Затем LLM должна будет сгенерировать последующие вопросы от каждого отдела для уточнения и улучшения предложения и идеи продукта.

## Обучение на этом не заканчивается, продолжайте путь

После завершения этого урока ознакомьтесь с нашей [коллекцией по генеративному ИИ](https://aka.ms/genai-collection?WT.mc_id=academic-105485-koreyst), чтобы продолжить повышать свои знания в области генеративного ИИ!

---

<!-- CO-OP TRANSLATOR DISCLAIMER START -->
**Отказ от ответственности**:
Этот документ был переведен с использованием сервиса машинного перевода [Co-op Translator](https://github.com/Azure/co-op-translator). Несмотря на наши усилия по обеспечению точности, имейте в виду, что автоматический перевод может содержать ошибки или неточности. Оригинальный документ на его исходном языке следует считать авторитетным источником. Для получения критически важной информации рекомендуется обратиться к профессиональному человеческому переводу. Мы не несем ответственности за любые недоразумения или неправильные толкования, возникшие в результате использования этого перевода.
<!-- CO-OP TRANSLATOR DISCLAIMER END -->